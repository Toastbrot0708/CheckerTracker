"""CheckerTracker scanner service, Python implementation.

Same contract as the Node service in the parent directory: real probes only,
no simulated data, scope and authorization enforced here rather than in the
UI. Standard library only, so it runs anywhere Python 3.8+ does — including
a-Shell on iOS and Termux on Android.
"""
